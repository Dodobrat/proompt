export enum AppRoute {
  Root = "/",
  //   NOT NEEDED FOR NOW
  //   Login = "/login",
  //   Register = "/register",
  Projects = "/projects",
  Project = "/projects/:id",
}

export enum AppSearchParamKeys {
  CreateProject = "createProject",
}
